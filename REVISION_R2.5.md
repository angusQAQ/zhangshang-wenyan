# R2.5 — 文言知識：合併全部內容（不分年級）

## 問題
`buildUnifiedKnowledge` 優先只取 **s3** 的 `html`，中一／中二正文被丟；畫面看起來「只有前中三那份」。

## 硬鎖
1. 主頁「文言知識」＝統一入口，**唔再分年級**（年級掣只服務篇章）。
2. 每一主題頁＝**s1＋s2＋s3 正文全部合併**（順序：入門→加深→銜接），可保留小節標籤方便掃讀，但唔做年級選擇。
3. `practice` 繼續三級去重合併（已做）。
4. 內容來源以 `data/knowledge.json` 既有三級為準（濃縮自 `source-wenyan-kit`／分級筆記）；**唔 verbatim 貼 PDF 全文**。
5. 合併後仍用 HTML 真字＋表，唔靠 chart PNG 帶可讀正文。

## 驗收
- 打開「文言虛詞」等任一主題，可見中一基礎八詞＋中二進階＋中三易混，三截齊。
- 小練題量 ≈ 三級合計去重後。
