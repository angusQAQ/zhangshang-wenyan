# Loop
開 App →（可調字級 小／中／大）→ 點年級 → 點模式（文言知識｜文言篇章｜文言字詞考核｜書籤）→ 點選閱讀／作答／收藏 → 即時對錯＋段解釋 → 下一題或返回。單次碎片 30–180 秒。刪「知識主題概覽」。中三佔位。

# 動詞
- **核心：點選**（年級／模式／列表／選項／書籤／色標字／顯示解釋）
- **修飾：即時對錯＋段落解釋（引原文）**（選完即判；對青綠／錯紅；下方段落寫清正解原因＋錯項原因，並引用篇章原文）
- 文言知識：點主題 → **表／圖為主**（教局官方＋用戶附件），禁文字牆；可入主題小測（仍 MCQ）
- 文言篇章：點篇目 → 讀原文 →「顯示解釋」→ 語譯／淺白／段注／主旨；重點字**色標**可點（粵音跟 A／B／C）→ 無限不重複 MCQ 操練
- 文言字詞考核：主頁直入；一句有出處文言句 → 問句中某詞義 → 四選一；題庫**唔同**現有篇章篇目
- 書籤：每題可釘；主頁書籤列表可回該題
- 禁拖拽玩法、禁倒數考試、禁第三戰鬥機制

# 空間（iPhone 直向）
1. **首頁**：Logo＋《掌上文言》＋年級三卡＋入口「文言字詞考核」「書籤」＋字級 小／中／大
2. **年級頁**：文言知識｜文言篇章 **置中**（刪知識主題概覽）
3. **知識**：主題列表 → 內容（圖表＋短表，可捲盡）
4. **篇章列表／內文**：必須可捲至最底，下方篇目／控件可點
5. **答題**：題幹＋A–D → 判後段落解釋 → 書籤鈕 → 下一題
6. **顯示解釋**面板：語譯｜淺白｜段注｜主旨｜色標字
7. **中三**：可入，列表只顯示「內容即將推出」

字級規則：切換小／中／大只改 `font-size`／`em`；圖表寬 100% 高 auto；**唔准**固定高度裁切導致破圖或整塊位移錯位。

## R2.1 字級（硬規格）
- 撳 **大／中／小** → **全 App 所有頁、所有文字**同步變（標題、正文、選項、解釋、按鈕字、色標字、badge）。
- 版面跟字級：**卡、列表列、捲區、按鈕、間距**用 `rem`／`em`／`%` 一齊脹縮；禁止固定 `px` 高度導致裁字／破圖／錯位。
- 圖表：寬 100%、高 auto；色標／icon 跟 rem，禁固定高裁切。
- 自測矩陣：三字級 × 首頁／模式／知識／篇章／答題／解釋／書籤；任一頁字唔變或破版＝FAIL。


# 勝／負／重開
- 一題勝／負：即時色態＋段落解釋（引原文）；錯唔踢走
- 篇章操練：**無題數上限**；抽題不重複至池盡再洗牌
- 字詞考核：同上（獨立題池）
- 重開／返回：回上一層或首頁；書籤跨會話可本機記（唔做雲端帳號）

# 前 10 秒
0s 見首頁＋年級。≤3s 點中一。≤6s 點置中「文言篇章」。≤10s 入列表或完成一次點選回饋。

# 難度旋鈕（≤3）
1. 年級（中一／中二／中三佔位）
2. 模式（知識／篇章／字詞考核）
3. 題型混比（語譯／主旨／判斷；判斷含「無從判斷」）

# 觸控對照
| 鍵鼠 | 觸控 | 作用 |
|------|------|------|
| 左鍵點 | 輕觸 | 年級／模式／選項／書籤／色標字／顯示解釋／下一題／返回 |
| 滾輪 | 直向滑動 | 列表、原文、解釋、知識圖表（必須捲盡） |

# Juice（意圖）
- 80ms 內按下態；對／錯色態分明；解釋區滑入
- 色標字輕觸弹出讀音層（依粵音方案）
- 無 BGM；點擊音可關

# 實體清單（≤6 主交互）
btn_grade｜btn_mode｜card_option｜panel_explain｜btn_bookmark｜btn_font_size  
（色標字＝原文內嵌熱點；顯示解釋＝panel 開關）

# 內容硬閘
- 知識：教局文件＋用戶附件；用表／圖展示；**不准**長文堆砌；**删**知識主題概覽
- 篇章題庫：用戶中一／中二包轉 MCQ；一局可連操；題目不重複至洗牌
- 選項解釋：段落、一針見血、**引用原文**
- 顯示解釋：語譯、淺白、段注、主旨齊
- 字詞考核句：必須有古典出處，且**唔重複**本 App 已收篇章
- 介面字：香港繁體書面語；禁粵語口語入 UI；禁簡體
- 粵音：等用戶 A 離線粵拼／B 連線中大庫／C 暫緩；未揀前色標可先亮、點讀跟方案

# 狀態
**R2＋R2.1＋P3 DESIGN**。粵音暫跟 A。字級 rem 硬閘已上；P3 書籤／無限題池／文言字詞考核已收。
