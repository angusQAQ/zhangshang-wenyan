# Loop
開 App →（可調字級 小／中／大）→ 二選一路徑：
- **文言知識**（主頁直入）→ 主題 → HTML 真字表／卡閱讀（可小測）
- **中一／中二／中三** → **直入篇章列表**（不再選「知識｜篇章」）→ 讀／顯示解釋／無限 MCQ
另：主頁「文言字詞考核」「書籤」。單次碎片 30–180 秒。中三篇章佔位。

# 動詞
- **核心：點選**（年級／知識入口／列表／選項／書籤／色標字／顯示解釋）
- **修飾：即時對錯＋段落解釋（引原文）**（選完即判；對青綠／錯紅；下方段落寫清正解原因＋錯項原因，並引用篇章原文）
- 文言知識：主頁入口 → 主題 → **HTML 真字＋CSS 表／卡**（跟根字級）；圖只做裝飾／色塊示意，**禁止 PNG 燒可讀正文**；可入主題小測（仍 MCQ）
- 文言篇章：年級卡 → 直入篇目列表 → 讀原文 →「顯示解釋」→ 語譯／淺白／段注／主旨；重點字**色標**可點（粵音 A）→ 無限不重複 MCQ 操練
- 文言字詞考核：主頁直入；一句有出處文言句 → 問句中某詞義 → 四選一；題庫**唔同**現有篇章篇目
- 書籤：每題可釘；主頁書籤列表可回該題
- 禁拖拽玩法、禁倒數考試、禁第三戰鬥機制

# 空間（iPhone 直向）
1. **首頁**：Logo＋《掌上文言》＋年級三卡（點＝篇章）＋入口「文言知識」「文言字詞考核」「書籤」＋字級 小／中／大
2. **（刪）年級模式頁**：不再有「文言知識｜文言篇章」二選一
3. **知識**：主題列表 → 內容（DOM 表／卡＋可選 deco 頭圖，可捲盡）；表內字跟根字級
4. **篇章列表／內文**：年級直入；必須可捲至最底
5. **答題**：題幹＋A–D → 判後段落解釋 → 書籤鈕 → 下一題
6. **顯示解釋**面板：語譯｜淺白｜段注｜主旨｜色標字
7. **中三**：可入，列表只顯示「內容即將推出」

字級規則：切換小／中／大只改根 `font-size`；全站 `rem`／`em`／`%`；圖表寬 100% 高 auto；**唔准**固定高度裁切。

## R2.1 字級（硬規格）
- 撳 **大／中／小** → **全 App 所有頁、所有文字**同步變（標題、正文、選項、解釋、按鈕字、色標字、badge、**表內格字**）。
- 版面跟字級：卡、列表、捲區、按鈕、間距用 `rem`／`em`／`%`；禁固定 `px` 裁字。
- 自測矩陣：三字級 × 首頁／知識／篇章／答題／解釋／書籤／字詞；任一頁字唔變或破版＝FAIL。

## R2.2 知識真字（硬規格）
- 通假字／特點／一詞多義等**解說字＋表**必須 **HTML 真字＋CSS 表／卡**，跟根字級。
- **禁止**用點陣圖（PNG chart）承載可讀正文或表內長句。
- 圖只留裝飾／色塊示意（`deco_*`）；過渡期可暫時藏舊燒字圖。
- 自測：字級「大」時，表內字 ≥ 正文同級。

## R2.3 資訊架構（硬規格）
- 主頁新增獨立「文言知識」入口 → 現有知識流。
- 中一／中二／中三 → **直入篇章列表**；刪中間模式選擇頁。
- 難度旋鈕改為：年級（篇章）｜知識主題｜題型混比；不再靠「模式二選一」分流。

# 勝／負／重開
- 一題勝／負：即時色態＋段落解釋（引原文）；錯唔踢走
- 篇章操練：**無題數上限**；抽題不重複至池盡再洗牌
- 字詞考核：同上（獨立題池）
- 重開／返回：回上一層或首頁；書籤跨會話可本機記（唔做雲端帳號）

# 前 10 秒
0s 見首頁＋年級＋「文言知識」。≤3s 點中一 → **直入篇章列表**。≤10s 入一篇或完成一次點選回饋。
（知識路徑：≤3s 點「文言知識」→ 主題列表。）

# 難度旋鈕（≤3）
1. 年級（中一／中二／中三佔位）→ 篇章深度
2. 知識主題（主頁「文言知識」）
3. 題型混比（語譯／主旨／判斷；判斷含「無從判斷」）

# 觸控對照
| 鍵鼠 | 觸控 | 作用 |
|------|------|------|
| 左鍵點 | 輕觸 | 年級／知識入口／選項／書籤／色標字／顯示解釋／下一題／返回 |
| 滾輪 | 直向滑動 | 列表、原文、解釋、知識表卡（必須捲盡） |

# Juice（意圖）
- 80ms 內按下態；對／錯色態分明；解釋區滑入
- 色標字輕觸弹出讀音層（粵音 A）
- 無 BGM；點擊音可關

# 實體清單（≤6 主交互）
btn_grade｜btn_knowledge｜card_option｜panel_explain｜btn_bookmark｜btn_font_size  
（色標字＝原文內嵌熱點；顯示解釋＝panel 開關；btn_mode 已删）

# 內容硬閘
- 知識：教局文件＋用戶附件；**HTML 表／卡**展示；删知識主題概覽；删燒字圖承載正文
- 篇章題庫：用戶中一／中二包轉 MCQ；一局可連操；題目不重複至洗牌
- 選項解釋：段落、一針見血、**引用原文**
- 顯示解釋：語譯、淺白、段注、主旨齊
- 字詞考核句：必須有古典出處，且**唔重複**本 App 已收篇章
- 介面字：香港繁體書面語；禁粵語口語入 UI；禁簡體
- 粵音：暫跟 A（離線粵拼）

# 狀態
**R2.2＋R2.3 DESIGN 已改**。R2.2：知識真字表優先；R2.3：主頁知識入口＋年級直入篇章。粵音 A。待 GIDEON／NOA 上線後驗。
