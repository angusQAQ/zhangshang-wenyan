# R2.10 — 字詞表疏朗對義／知識點選互動／組改中／練習跨年級

## 做了甚麼
1. **文言字詞表**：改卡片疏朗排版（加大 padding／行距／卡距）；多義／多例以 **①②③** 標號，**義／例／出處同號對齊**；`data/glossary.json` 重切為 `items[]`，並二次校對錯義／錯例／錯出處／OCR 異體。
2. **文言知識**：七主題皆有主人翁佔位（`hero_<topic>.png` → fallback `logo_master`）、色塊熱點、圖表槽；**互動僅「點選」**——熱點展開沉浸面板／色表列高亮／筆記卡點開；意動／使動對照卡同機制。NOA 資產路徑預留。
3. **文案**：組一／二／三 → **中一／中二／中三**（全站 UI）。
4. **練習入口**：底欄「練習」顯示中一＋中二＋中三**全部篇章**；主頁點年級卡仍只開該級。
5. 快取：`?v=r210`。

## 改檔
- `data/glossary.json`、`data/knowledge.json`
- `js/app.js`、`css/app.css`、`index.html`
- `REVISION_R2.10.md`

## 校對
- 字詞 150 條全數重切；多義／多例對齊約 100 條；義項或例句／出處修正約 30+ 處（詳見 commit message）。

## 風險
- NOA `hero_*`／`tap_hint_ring` 等未齊時靠 CSS＋logo 佔位；圖到位即自動換（onerror fallback）。
- 練習「全部篇章」列表較長，手機需捲動；主頁年級入口仍可分流。
- 知識 HTML 經 merge 後 s1 注入 hero，避免三級重複堆疊。
